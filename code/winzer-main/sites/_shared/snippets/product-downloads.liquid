{% comment %}

Usage: Intended usage is for the buy stack / product information section of the PDP, very similar to the addon-form

Parameters:

    product_documents: String - Formatted to be something like: product-handle1,product-handle2,product-handle3
    title: String - Heading

{% endcomment %}

<div class="product__accordion accordion" {{ block.shopify_attributes }}>
    <details>
        <summary>
            <div class="summary__title">
                <h2 class="h4 accordion__title">
                    {{ title | default: block.settings.page.title }}
                </h2>
            </div>
            <div class="status-indicator"></div>
        </summary>
        <div class="accordion__content rte">
            {% assign documents_sets = product_documents | split: '|' %}
            {% for document_set in documents_sets  %}
                {% assign index_parity = forloop.index | modulo: 2 %}
                {% if index_parity == 1 %}
                    {% assign document_name = document_set %}
                {% else %}
                    <div class="product-downloads">
                        <a href="{{ document_set }}" target="_blank">{{ document_name }}</a>
                        {% render 'icon-download' %}
                    </div>
                {% endif %}
            {% endfor %}
        </div>
    </details>
</div>